# Normalise to LF in the repo; check out native. Keeps diffs clean across
# platforms instead of showing whole-file changes on line endings alone.
* text=auto eol=lf

# Binary — never touch these.
*.webp binary
*.jpg  binary
*.png  binary
